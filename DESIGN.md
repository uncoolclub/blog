# 디자인

조용한 정밀함. 672px 한 컬럼, 뉴트럴 그레이, 블루 하나.
장식은 없고, 색이 귀할수록 눈에 띈다는 원칙으로 블루를 아껴 쓴다.

## 색

| 토큰 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| `--bg` | `#ffffff` | `#111113` | 지면 |
| `--bg-soft` | `#f6f6f8` | `#1b1b1f` | 표면 (간트 트랙 등) |
| `--chip` | `#f2f2f4` | `#232327` | 칩, 인라인 코드 |
| `--border` | `#eaeaee` | `#26262b` | 헤어라인 |
| `--text` ~ `--text-4` | `#19191c` ~ `#b0b0b8` | 대응 밝음 | 텍스트 4단계 |
| `--accent` | `#1342ff` | `#1342ff` | 로고, 링크, 진행률 바 |

그레이는 전부 뉴트럴(노랑 기 없음). 예외적 웜 톤은 소개 아바타 자리 표시와
커버 그라디언트 두 곳뿐이다.

## 타이포

- 본문: system-ui 스택 (SF Pro / Apple SD Gothic Neo / Pretendard). 16.5px, 행간 1.85
- 라벨•날짜•메타: Space Grotesk (Google Fonts). 코드 폰트가 아니면서 숫자에 개성이 있다
- 코드: SF Mono 스택 — 코드 블록과 인라인 코드에만
- 제목: 글 상세 29px/650, 홈 대표 카드 22px/650

## 마크

별(스타버스트) 하나가 브랜드 전부다. path 정본은 `svgs/star-mark.tsx`.

- 헤더: 블루 별, 16초에 한 바퀴 도는 회전 (reduced-motion에서 정지)
- 푸터: 다크에서 흰색, 라이트에서 회색 (`--star-muted`)
- 파비콘: 같은 별의 SVG

## 페이지

- 홈: 최신 글 대형 카드(본문 첫 이미지 = 커버) → 2열 카드 → 리스트.
  이미지 없는 글은 id 기반 모티프 커버(`svgs/covers.tsx`)로 폴백
- 글 상세: 상단 2.5px 블루 진행률 바, 제목 + `날짜 · N words`, 본문,
  이전/다음 고스트 카드, giscus(설정 시)
- 소개: 아바타 + 소개 문단 + Experience 간트(회사색 라벨 필) + Contact
- 본문 규칙: 섹션 h2 위 72px, 이미지 상하 40px, 인용은 블루 바 + 본문색

## 모션

hover는 색 전이만. 유일한 상시 모션은 헤더 별의 회전이고,
진행률 바는 스크롤 핸들러가 리렌더 없이 DOM을 직접 갱신한다.
